# GM7STUDIO — موقع جلال الشخصي

هذا مستودع Git مرتبط مباشرة بـ https://github.com/gm7morey/GM7STUDIO (remote: `origin`, الفرع الرئيسي: `master`).

## سياسة الحفظ التلقائي على GitHub

بطلب صريح من المستخدم: أي تعديل يقوم به Claude على ملفات هذا الموقع يجب أن يُحفظ تلقائياً على GitHub بدون انتظار تأكيد إضافي في كل مرة:

1. بعد إجراء تغيير (أو مجموعة تغييرات مترابطة)، نفّذ:
   - `git add <الملفات المعدّلة>`
   - `git commit -m "<رسالة موجزة بالعربية أو الإنجليزية تصف التغيير>"`
   - `git push origin master`
2. لا داعي لسؤال المستخدم "هل أرفع التغييرات؟" — هذا مفوّض مسبقاً لهذا المستودع تحديداً.
3. استثناء: إذا كان التغيير تجريبياً أو غير مكتمل ويطلب المستخدم عدم رفعه، انتظر تعليماته.
4. لا تستخدم `--force` أو `git reset --hard` أو أي أمر مدمّر بدون تأكيد صريح من المستخدم — هذا التفويض يغطي الحفظ العادي (add/commit/push) فقط.
